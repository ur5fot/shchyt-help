# Покращення golden set + HyDE для розмитих запитів

## Overview
Два покращення: (1) виправити неточні expectedChunks у golden set та додати синоніми для слабких категорій, (2) додати HyDE (Hypothetical Document Embeddings) — для неясних запитів генерувати гіпотетичну відповідь через Claude і шукати по її ембеддингу. Мета: підняти recall з 69.6% до 80%+.

## Context
- Re-ranking підняв recall з 42.9% до 69.6%, 17 питань ще пропускаються
- Слабкі категорії: грошове забезп. (45%), переведення (50%), воєнний стан (67%)
- Деякі expectedChunks можуть вказувати не на той чанк (хибні промахи)
- HyDE допоможе з розмитими запитами типу "що робити якщо затримують виплату"

## Development Approach
- **Testing approach**: Regular (code first, then tests)
- **CRITICAL: всі тести мають проходити перед початком наступного таску** (`npm test`)
- **CRITICAL: запустити `npm run eval` до і після для порівняння**

## Progress Tracking
- Завершені елементи позначати `[x]` одразу

## Implementation Steps

### Task 1: Аудит та виправлення golden set
- [x] Для кожного з 17 пропущених питань перевірити: чи expectedChunk справді найрелевантніший чанк
- [x] Шукати правильні chunk ID через `grep` по тексту законів — можливо потрібен інший чанк
- [x] Виправити expectedChunks де вони вказують не на той чанк
- [x] Додати альтернативні expectedChunks де є кілька правильних відповідей (масив ID)
- [x] Запустити `npm run eval` — перевірити що recall зріс за рахунок виправлень
- [x] `npm test` — всі тести проходять

### Task 2: Додати синоніми для слабких категорій
- [ ] Проаналізувати 17 пропущених питань — які слова з запиту не знаходять потрібних чанків
- [ ] Додати нові синоніми в `lawSearch.ts` для:
  - Грошове забезпечення: "посадовий оклад", "надбавка", "доплата", "розмір виплати"
  - Мобілізація: "повістка", "відстрочка", "бронювання", "вік"
  - Переведення: "переміщення", "нижча посада", "контракт"
- [ ] Запустити `npm run eval` — перевірити покращення
- [ ] `npm test` — всі тести проходять

### Task 3: Створити HyDE модуль (`server/src/services/hyde.ts`)
- [ ] Функція `generateHypothesis(query: string): Promise<string>` — відправляє запит до Claude з промптом: "Напиши коротку відповідь (2-3 речення) на це питання з позиції військового права, згадай конкретні статті"
- [ ] Промпт для HyDE має бути коротким і дешевим (max_tokens: 200)
- [ ] Використовувати окремий system prompt (не основний — менший, без форматування)
- [ ] Кешувати: якщо запит < 15 символів або однакові слова — не генерувати hypothesis
- [ ] Graceful fallback: якщо Claude API помилка — шукати по оригінальному запиту
- [ ] Написати тести з мок Claude API
- [ ] `npm test` — всі тести проходять

### Task 4: Інтегрувати HyDE в пошук
- [ ] В `hybridSearchLaws` — якщо увімкнений HyDE: генерувати hypothesis, створити ембеддинг hypothesis, шукати по ньому в додаток до оригінального запиту
- [ ] Об'єднати результати: оригінальний пошук + HyDE пошук → дедуплікація → re-ranking
- [ ] Додати константу `HYDE_УВІМКНЕНИЙ = true` в constants.ts
- [ ] HyDE додає один виклик Claude API (~200 токенів) — прийнятна ціна
- [ ] Логувати: "HyDE: hypothesis згенеровано за Nмс"
- [ ] Написати тести
- [ ] `npm test` — всі тести проходять

### Task 5: Eval та документація
- [ ] Запустити `npm run eval` — порівняти recall до і після
- [ ] Оновити `CLAUDE.md` — додати опис HyDE
- [ ] Позначити HyDE як реалізований у розділі "Плановані покращення"
- [ ] `npm test` — всі тести проходять
- [ ] `npm run lint` — без помилок

## Verification
1. `npm run eval` → recall вищий ніж 69.6%
2. `npm test` → всі тести проходять
3. HyDE graceful fallback працює (вимкнути API ключ → пошук працює без HyDE)
4. Час відповіді: HyDE додає ~1-2с (один короткий Claude API виклик)
